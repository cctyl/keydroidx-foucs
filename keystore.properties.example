# 复制本文件为 keystore.properties 并填入真实值。
# keystore.properties 已被 .gitignore 忽略，切勿提交；密钥文件（*.jks）同样不要提交。
# 未配置时构建会回退到 AGP 默认的 debug 签名，干净克隆也能 assembleDebug。
RELEASE_STORE_FILE=test.jks
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=key0
RELEASE_KEY_PASSWORD=
